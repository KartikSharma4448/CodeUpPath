{
  "version": 2,
  "buildCommand": "tsx script/generate-sitemap.ts && VERCEL=1 vite build && node script/build-api.mjs",
  "outputDirectory": "public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
